Я не стала разбивать эту задачу на несколько пожзадач, так как не видела в этом надобности.

Первым делом в методе я вычисляю, сколько в заданном массиве слов, в которых содержится меньше чем четыре символа.
Делается это благодаря тому, что тип string сам по себе является массивом char'ов, чем я и воспользовалась.
Найденное значение становится размером для нового массива тех самых слов.
Важно отметить, что при присвоении новому массиву значений старого массива важно не выйти за рамки индексации искомого массива, потому и был введен счётчик k.
Ну и всё.

Из минусов правда есть то, что этот алгоритм исходный массив два раза перебирает, но я пока не знаю, как при первом переборе заполнять искомый массив, так как мне нужно знать его размер заранее.
